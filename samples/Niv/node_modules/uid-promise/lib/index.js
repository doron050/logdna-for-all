"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
// Packages
const crypto_1 = require("crypto");
// Utilities
const chars_1 = __importDefault(require("./chars"));
const uuid = (len) => new Promise((resolve, reject) => {
    if (!Number.isInteger(len)) {
        reject(new TypeError('You must supply a length integer to `uid-promise`.'));
        return;
    }
    if (len <= 0) {
        reject(new Error('You must supply a length integer greater than zero'));
        return;
    }
    crypto_1.randomBytes(len, (err, buf) => {
        if (err) {
            return reject(err);
        }
        const str = [];
        let rand;
        for (let i = 0; i < buf.length; i++) {
            rand = buf[i];
            while (rand > 248) {
                try {
                    rand = crypto_1.randomBytes(1)[0];
                }
                catch (err) {
                    reject(err);
                }
            }
            str.push(chars_1.default[rand % chars_1.default.length]);
        }
        resolve(str.join(''));
    });
});
exports.default = uuid;
module.exports = uuid;
